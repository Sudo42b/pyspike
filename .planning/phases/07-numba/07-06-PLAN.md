---
phase: 07-numba
plan: 06
type: execute
wave: 2
depends_on: [07-05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - README.md
  - pyproject.toml
autonomous: true
requirements: [NJIT-08, NJIT-07]
requirements_addressed: [NJIT-08, NJIT-07]
gap_closure: false
must_haves:
  truths:
    - "REQUIREMENTS.md `Out of Scope` numba row replaced verbatim with the D-04 reword: 'numba는 v1 hard dependency 제외 (Phase 7의 optional `spike[fast]` extras 통한 lazy 가속은 허용). cython / JAX / torch / scipy는 v1 전 영역에서 hard 또는 optional dep 모두 제외.'"
    - "REQUIREMENTS.md adds new `### Numba Optimization (Phase 7)` section with NJIT-01..NJIT-08 entries"
    - "REQUIREMENTS.md Traceability table updated to map NJIT-01..NJIT-08 → Phase 7 with status (Complete or In Progress depending on Plan 05 outcome)"
    - "PROJECT.md Constraints section gains a new 'Wheel size policy' bullet clarifying base ≤50MB + extras [fast] transitive unconstrained (P7 D-15); ROADMAP.md Phase 6 success criteria #4 'wheel size ≤50MB' updated to 'base wheel size ≤50MB' with extras carve-out (B2 fix — original PROJECT.md has no '50MB' string; cap lives in ROADMAP.md)"
    - "ROADMAP.md Phase 7 section 'Goal: [To be planned]' replaced with derived goal + Requirements list (NJIT-01..NJIT-08) + Success Criteria + Plans table"
    - "README.md adds new top-level 'Performance acceleration' section explaining `pip install spike[fast]`"
    - "pyproject.toml adds `[tool.cibuildwheel] test-extras = [\"fast\"]` so CI installs numba and exercises JIT path"
    - "Doc sync verifiable: `grep -q 'optional spike\\[fast\\]' .planning/REQUIREMENTS.md && grep -q 'base wheel size' .planning/PROJECT.md && grep -q 'NJIT-' .planning/REQUIREMENTS.md && grep -q 'pip install spike\\[fast\\]' README.md`"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Out of Scope numba row reworded; new Numba Optimization (Phase 7) section with NJIT-01..NJIT-08; Traceability table extended"
      contains: "NJIT-01"
    - path: ".planning/PROJECT.md"
      provides: "Wheel size constraint clarified to 'base wheel size ≤50MB' with extras carve-out"
      contains: "base wheel size"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 section Goal/Requirements/Success Criteria/Plans filled (replaces 'TBD' placeholders)"
      contains: "NJIT-08"
    - path: "README.md"
      provides: "New 'Performance acceleration' section explaining `pip install spike[fast]` opt-in"
      contains: "spike[fast]"
    - path: "pyproject.toml"
      provides: "[tool.cibuildwheel] test-extras = [\"fast\"] added"
      contains: "test-extras"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "v1 ship gate documentation closure"
      via: "Out of Scope reword + new Phase 7 section + Traceability table"
      pattern: "NJIT-08"
    - from: ".planning/PROJECT.md"
      to: "Wheel size constraint clarification"
      via: "'wheel size ≤50MB' → 'base wheel size ≤50MB'"
      pattern: "base wheel size"
    - from: "pyproject.toml [tool.cibuildwheel]"
      to: "CI verifies the JIT path on cp310-cp312"
      via: "test-extras = [\"fast\"]"
      pattern: "test-extras = \\[\"fast\"\\]"
---

<objective>
**Wave 2 Plan 06 (sequential after Wave 1b Plan 05 green):** Doc + CI sync work to close the v1 ship gate.

Purpose: This plan changes NO source code or tests; it synchronizes the planning documents (REQUIREMENTS.md, PROJECT.md, ROADMAP.md), README, and CI config (`[tool.cibuildwheel]`) with the realized P7 outcome. Specifically:
1. **REQUIREMENTS.md** — reword the `Out of Scope` numba row (D-04) so it no longer conflicts with P7; add a new `### Numba Optimization (Phase 7)` section listing NJIT-01..NJIT-08; extend the Traceability table.
2. **PROJECT.md** — clarify "wheel size ≤50MB" to "base wheel size ≤50MB" so the [fast] extras carve-out is explicit (D-15).
3. **ROADMAP.md** — replace Phase 7 placeholder ("Goal: [To be planned]" + "Plans: 0 plans") with the realized goal, requirements, success criteria, and 6-plan list.
4. **README.md** — add a "Performance acceleration" section so users know about `pip install spike[fast]`.
5. **pyproject.toml** — add `[tool.cibuildwheel] test-extras = ["fast"]` so CI installs numba in the test environment and exercises the JIT path on cp310-cp312 manylinux2014_x86_64 (NJIT-07 CI verification).

Output: 5 files modified.

Wave gating rationale: depends on Plan 05 (NJIT-04 sweep + NJIT-06 5x walltime gates GREEN) so the documentation can accurately state v1 ship readiness. The doc sync intentionally happens AFTER all code is green to avoid claiming "complete" before observable evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-numba/07-CONTEXT.md
@.planning/phases/07-numba/07-RESEARCH.md
@.planning/phases/07-numba/07-VALIDATION.md
@.planning/phases/07-numba/07-01-PLAN.md
@.planning/phases/07-numba/07-02-PLAN.md
@.planning/phases/07-numba/07-03-PLAN.md
@.planning/phases/07-numba/07-04-PLAN.md
@.planning/phases/07-numba/07-05-PLAN.md
@README.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md (reword Out of Scope + add NJIT section + Traceability)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — locate Out of Scope table + Traceability table + section headers)
    - .planning/phases/07-numba/07-CONTEXT.md D-04 section (canonical reword text)
    - .planning/phases/07-numba/07-CONTEXT.md "Derived P7 REQ IDs" table (NJIT-01..NJIT-08 canonical descriptions)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` in three places:

    **Edit 1 — `Out of Scope` table row (D-04 reword):**

    Find the line:
    ```
    | numba / cython / JAX / torch / scipy | NumPy 단독으로 회귀 시간 예산 충족. 추가 시 wheel 빌드 복잡도/사이즈 폭증 |
    ```

    Replace with:
    ```
    | numba (hard dep, CUDA path) | numba는 v1 hard dependency 제외 (Phase 7의 optional `spike[fast]` extras 통한 lazy 가속은 허용). |
    | cython / JAX / torch / scipy | v1 전 영역에서 hard 또는 optional dep 모두 제외. 추가 시 wheel 빌드 복잡도/사이즈 폭증 |
    ```

    (Two rows replace the original one. The first row carves out numba [fast] extras; the second preserves the original rejection for cython/JAX/torch/scipy.)

    **Edit 2 — Add new `### Numba Optimization (Phase 7)` section under v1 Requirements:**

    Locate the position immediately after the `### Distribution (Wheel packaging)` subsection (after PKG-04 line) and BEFORE `## v2 Requirements`. Insert:

    ```markdown
    ### Numba Optimization (Phase 7)

    Phase 7 layers optional numba JIT acceleration over the NumPy-only base wheel.
    All NJIT requirements are gated by P6 strict-mode regression green; the
    extras (`pip install spike[fast]`) are opt-in.

    - [ ] **NJIT-01**: Lazy `from numba import njit` + auto NumPy fallback (`HAS_NUMBA` gate). `pip install spike` (base) → NumPy-only operation; `pip install spike[fast]` → numba acceleration. Both paths must pass full P6 strict-mode regression. (D-02)
    - [ ] **NJIT-02**: 28 stateless kernels (`gemm_core` 3 + `vec_core` 7 + `act_core` 18) decorated with `@njit(cache=True)` via `_jit.py` shim with FP32-only `_impl` signatures (numba CPU does not support np.float16). Engine layer (mm/vec/act_engine) untouched. (D-06 + NJIT-FP32-BOUNDARY)
    - [ ] **NJIT-03**: `fastmath=False` (numba default) + `with numba.objmode(...)` escape for 5 transcendental kernels (gelu, tanh_act, sigmoid, softmax, esum). Without objmode, LLVM `tanhf`/`expf` differ from glibc by ~1 ULP causing 9/1024 GELU FP16 mismatches; with objmode, 0/1024. (D-09 refined)
    - [ ] **NJIT-04**: Vendor 84-op directory full sweep gate. `tests/gtx/test_regression_fw_full_sweep.py` parametrize over `vendor/gtx_cpp_reference/test/<OP>/n1s16/`; strict-mode `compare_hex(strict=True)` PASS for ops with both .elf + golden present, `pytest.skip` graceful otherwise. M passed + N skipped == 84. (D-10 refined)
    - [ ] **NJIT-05**: Per-kernel ULP-0 parity test (Tier 1). `tests/gtx/test_njit_parity.py` — 28 kernels × NumPy vs JIT delta_ulp == 0 via `np.array_equal(out.view(np.uint16), out_njit.view(np.uint16))`. Transcendental kernels pass via objmode escape. (D-12)
    - [ ] **NJIT-06**: Wall-clock 5× walltime acceptance (Tier 3). `tests/gtx/test_njit_perf.py` pytest-benchmark — full vendor 84-op sweep walltime ≤ 1/5 of P6 NumPy-only baseline. Baseline locked in `tests/gtx/data/baseline_walltime.txt`. (D-13)
    - [ ] **NJIT-07**: Extras packaging. `pyproject.toml [project.optional-dependencies]` adds `fast = ["numba>=0.61.2,<0.66"]` (raised from CONTEXT D-05 `>=0.59` because 0.59 pins numpy<1.27 conflicting with our numpy>=2.0 floor). cibuildwheel `test-extras = ["fast"]` so CI verifies the JIT path on cp310-cp312. (D-03 + D-05 refined)
    - [ ] **NJIT-08**: Documentation sync. REQUIREMENTS.md `Out of Scope` numba row reworded (this section); PROJECT.md "wheel size ≤50MB" clarified to "base wheel size ≤50MB"; ROADMAP.md Phase 7 section filled with goal/requirements/plans; README.md "Performance acceleration" section added. (D-04 + D-15)
    ```

    **Edit 3 — Extend Traceability table:**

    At the end of the existing Traceability table (after the PKG-04 row), append:

    ```
    | NJIT-01 | Phase 7 | Pending |
    | NJIT-02 | Phase 7 | Pending |
    | NJIT-03 | Phase 7 | Pending |
    | NJIT-04 | Phase 7 | Pending |
    | NJIT-05 | Phase 7 | Pending |
    | NJIT-06 | Phase 7 | Pending |
    | NJIT-07 | Phase 7 | Pending |
    | NJIT-08 | Phase 7 | Pending |
    ```

    Update Coverage:
    - Find the line: `- v1 requirements: 42 total` and change to `- v1 requirements: 50 total`.
    - Find: `- Mapped to phases: 42 ✓` and change to `- Mapped to phases: 50 ✓`.
    - Update phase distribution: append `Phase 7 (Numba Optimization): 8 (NJIT-01..NJIT-08)`.

    **S2 lifecycle note:** All 8 NJIT entries land as `Pending` in this plan. Status transitions to `Complete` happen in `/gsd:complete-phase 7` workflow (separate command — not Plan 06's responsibility). Plan 06's job is to LAND the requirements as documented; Phase completion certifies them.

    **Verification of edit precision:** after editing, `grep -c "^- \\[ \\] \\*\\*NJIT-" .planning/REQUIREMENTS.md` should return 8 (one per NJIT requirement).
  </action>
  <verify>
    <automated>grep -c "NJIT-" .planning/REQUIREMENTS.md &amp;&amp; grep -q "optional .spike\\[fast\\]" .planning/REQUIREMENTS.md &amp;&amp; grep -c "NJIT-08" .planning/REQUIREMENTS.md &amp;&amp; grep -c "v1 requirements: 50 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NJIT-" .planning/REQUIREMENTS.md` returns >= 17 (8 NJIT entries + 8 Traceability rows + headers).
    - `grep -q "optional .spike\\[fast\\]" .planning/REQUIREMENTS.md` exits 0.
    - `grep -q "Numba Optimization (Phase 7)" .planning/REQUIREMENTS.md` exits 0.
    - `grep -c "^- \\[ \\] \\*\\*NJIT-" .planning/REQUIREMENTS.md` returns exactly 8.
    - `grep -q "v1 requirements: 50 total" .planning/REQUIREMENTS.md` exits 0.
    - The original Out of Scope numba row is gone: `grep -c "NumPy 단독으로 회귀 시간 예산 충족" .planning/REQUIREMENTS.md` returns 0.
    - The reword landed: `grep -q "v1 hard dependency 제외" .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <done>REQUIREMENTS.md has Out of Scope reword, new NJIT-01..NJIT-08 section, extended Traceability, and updated Coverage count.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md (clarify wheel size to base-only) + ROADMAP.md (fill Phase 7 placeholders) + pyproject.toml CI test-extras</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, pyproject.toml</files>
  <read_first>
    - .planning/PROJECT.md (locate "wheel size" / "50MB" mentions)
    - .planning/ROADMAP.md (locate Phase 7 section: line 224 "### Phase 7:" — "Goal: [To be planned]")
    - .planning/phases/07-numba/07-CONTEXT.md D-15 (base wheel + extras unconstrained)
    - .planning/phases/07-numba/07-CONTEXT.md "Derived P7 REQ IDs" + research recommended plan structure
    - pyproject.toml (locate existing [tool.cibuildwheel.linux] section)
  </read_first>
  <action>
    **Edit 1 — PROJECT.md + ROADMAP.md "wheel size ≤50MB" clarification (B2 fix):**

    **Pre-flight check (REQUIRED):** Run `grep -n "50MB\|≤50\|wheel size" .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` to identify exact host. Empirical state: PROJECT.md has NO "50MB" string; ROADMAP.md Phase 6 success criteria #4 (~line 177) hosts it: `wheel size is ≤50MB (or split into spike[gtx-regression] extra if exceeded)`.

    **Action #1 — PROJECT.md (ADD action, not find/replace):** Append a new clause to the existing `## Constraints` section under `**Dependencies**` bullet. Locate:
    ```
    - **Dependencies**: NumPy 외부 추가 런타임 의존성 신규 도입 금지(wheel 배포 단순성).
      검증 단계에서만 기존 C++ libgtx_npu.so 참조(개발 환경)
    ```

    Insert IMMEDIATELY AFTER this bullet (before `**Bit-exact**`):
    ```
    - **Wheel size policy**: base wheel size ≤50MB (NumPy-only baseline; PKG-04 / Phase 6
      success criteria). Optional `[fast]` extras (`pip install spike[fast]`)는 numba +
      llvmlite transitive size 무제한 허용 (P7 D-15; 약 50–80MB 추가). cibuildwheel
      매트릭스는 base wheel만 검증; extras transitive는 사용자 opt-in이므로 size 제약
      없음.
    ```

    **Action #2 — ROADMAP.md Phase 6 success criteria #4:** Find the current line (`~line 177`):
    ```
    4. `cibuildwheel` matrix builds green for **cp310–cp312** manylinux2014_x86_64 (Phase 1 D-08; cp38/cp39 dropped) with no regressions vs the pre-GTX baseline; wheel size is ≤50MB (or split into `spike[gtx-regression]` extra if exceeded).
    ```

    Replace `wheel size is ≤50MB (or split into spike[gtx-regression] extra if exceeded).` with:
    ```
    base wheel size is ≤50MB (extras [fast] transitive size unconstrained per P7 D-15 — base + numba + llvmlite ≈ 50–80MB).
    ```

    **Acceptance check (B2):** `grep -q "base wheel size" .planning/PROJECT.md` exits 0 AND `grep -q "extras .fast." .planning/PROJECT.md` exits 0 AND `grep -q "base wheel size" .planning/ROADMAP.md` exits 0.

    **Edit 2 — ROADMAP.md Phase 7 section:**

    Find the section starting `### Phase 7: 제대로 동작을 하면, numba등의 라이브러리를 통해 동적 최적화 기술을 이용하여 최적화 해줘.` (line 224) and replace the four lines after that header:

    Original:
    ```
    **Goal:** [To be planned]
    **Requirements**: TBD
    **Depends on:** Phase 6
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd:plan-phase 7 to break down)
    ```

    Replace with:
    ```
    **Goal**: 28 stateless GTX NPU kernels (`gemm_core` 3 + `vec_core` 7 + `act_core` 18) accelerate via optional numba `@njit(cache=True)` lazy import with auto NumPy fallback; vendor 84-op `n1s16` regression sweep passes strict-mode (M passed + N skipped == 84); wall-clock walltime is at least 5x faster than P6 NumPy-only baseline; base wheel remains NumPy-only with `pip install spike[fast]` opt-in extras.

    **Depends on**: Phase 6 (P6 strict-mode regression green is hard prerequisite)

    **Requirements**: NJIT-01, NJIT-02, NJIT-03, NJIT-04, NJIT-05, NJIT-06, NJIT-07, NJIT-08

    **Success Criteria** (what must be TRUE):
      1. `pytest tests/gtx/test_njit_parity.py -v --no-cov` reports 28/28 kernels PASS with `np.array_equal(out.view(np.uint16), out_njit.view(np.uint16))` (delta_ulp == 0 across all 28).
      2. `pytest tests/gtx/test_regression_fw_full_sweep.py -v --no-cov` reports M passed + N skipped where M+N == 84 (full vendor `n1s16` directory accounted for; M >= 12 on developer machine with 12 P5/P6 .elf bundled, M >= 60 with `/opt/riscv/` toolchain present).
      3. `pytest tests/gtx/test_njit_perf.py --benchmark-only --benchmark-warmup=on --benchmark-warmup-iterations=3` PASSes the 5x walltime assertion (`benchmark.stats['mean'] * 5 <= baseline_walltime`).
      4. `pip install spike` (base) AND `pip install spike[fast]` BOTH produce a working `from riscv.gtx import GtxNpu` and pass full P6 regression sweep (NumPy fallback path equally bit-exact).
      5. `[tool.cibuildwheel] test-extras = ["fast"]` exists in pyproject.toml; cibuildwheel matrix builds green for cp310-cp312 with numba installed in test env.

    **Plans:** 6 plans (Wave 0 scaffold + 3 parallel Wave 1a kernel rewrites + Wave 1b sweep/perf integration + Wave 2 doc sync)
    - [ ] 07-numba/07-01-PLAN.md — Wave 0 scaffold: `_jit.py` shim + pyproject.toml extras + RED test scaffolds + 84-op sweep skeleton — covers NJIT-01 + NJIT-07 (extras part)
    - [ ] 07-numba/07-02-PLAN.md — Wave 1a gemm_core.py FP32-only `_impl` + njit + 3 parity tests GREEN — NJIT-02 (gemm) + NJIT-05 (gemm)
    - [ ] 07-numba/07-03-PLAN.md — Wave 1a vec_core.py FP32-only `_impl` + njit + 7 parity tests GREEN — NJIT-02 (vec) + NJIT-05 (vec)
    - [ ] 07-numba/07-04-PLAN.md — Wave 1a act_core.py FP32-only `_impl` + njit + 5 transcendentals via objmode + 18 parity tests GREEN — NJIT-02 (act) + NJIT-03 + NJIT-05 (act)
    - [ ] 07-numba/07-05-PLAN.md — Wave 1b vendor 84-op golden import + 72 .elf builds + sweep test GREEN + perf benchmark — NJIT-04 + NJIT-06
    - [ ] 07-numba/07-06-PLAN.md — Wave 2 doc + CI sync (REQUIREMENTS.md / PROJECT.md / ROADMAP.md / README.md / cibuildwheel test-extras) — NJIT-08 + NJIT-07 (CI part)
    **UI hint**: no
    ```

    **Edit 3 — pyproject.toml `[tool.cibuildwheel]` test-extras:**

    Find the existing block:
    ```
    [tool.cibuildwheel.linux]
    before-all = "yum install -y dtc && git submodule update --init --recursive"
    ```

    Add a new top-level `[tool.cibuildwheel]` block immediately above (or below, but consistent ordering) it:
    ```
    [tool.cibuildwheel]
    test-extras = ["fast"]
    test-command = "pytest {project}/tests/gtx -m 'not slow' -x --no-cov"
    ```

    The `test-extras = ["fast"]` instructs cibuildwheel to install the `fast` optional dependency (which we declared in Plan 01) when running the test command in the build sandbox. The `test-command` is a sentinel that runs the gtx test suite without the slow benchmark tier.

    **Note:** if `[tool.cibuildwheel]` already exists (without these keys), MERGE the new keys into the existing block. Otherwise add as new block. Inspect first.
  </action>
  <verify>
    <automated>grep -c "base wheel size" .planning/PROJECT.md &amp;&amp; grep -c "base wheel size" .planning/ROADMAP.md &amp;&amp; grep -c "NJIT-08" .planning/ROADMAP.md &amp;&amp; grep -c "test-extras = \\[\"fast\"\\]" pyproject.toml &amp;&amp; grep -A 5 "Phase 7:" .planning/ROADMAP.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "base wheel size" .planning/PROJECT.md` returns >= 1 (B2: new "Wheel size policy" bullet added).
    - `grep -c "extras" .planning/PROJECT.md` returns >= 1 (B2: extras carve-out clause present).
    - `grep -c "base wheel size" .planning/ROADMAP.md` returns >= 1 (B2: ROADMAP.md Phase 6 #4 updated).
    - The original ROADMAP "wheel size is ≤50MB (or split into" phrasing is gone: `grep -c "or split into .spike\[gtx-regression" .planning/ROADMAP.md` returns 0.
    - `grep -c "NJIT-01" .planning/ROADMAP.md` returns >= 1.
    - `grep -c "NJIT-08" .planning/ROADMAP.md` returns >= 1.
    - `grep -c "07-01-PLAN.md" .planning/ROADMAP.md` returns >= 1.
    - `grep -c "07-06-PLAN.md" .planning/ROADMAP.md` returns >= 1.
    - The placeholder `[To be planned]` for Phase 7 is gone: `grep -c "Phase 7.*\\[To be planned\\]" .planning/ROADMAP.md` returns 0.
    - `grep -c "test-extras = \\[\"fast\"\\]" pyproject.toml` returns >= 1.
    - `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'fast' in d['tool']['cibuildwheel']['test-extras']"` exits 0.
  </acceptance_criteria>
  <done>PROJECT.md wheel-size constraint clarified to base-only; ROADMAP.md Phase 7 section filled with goal/requirements/criteria/plans; cibuildwheel test-extras configured.</done>
</task>

<task type="auto">
  <name>Task 3: Add `Performance acceleration` section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file — find appropriate insertion point; typically after Installation or Usage section)
    - .planning/phases/07-numba/07-CONTEXT.md "Specifics > pip install spike[fast] 사용자 시나리오" section (canonical user-facing copy)
    - .planning/phases/07-numba/07-RESEARCH.md §"Empirical Speedup Evidence" (455x gemm; 5x sweep)
  </read_first>
  <action>
    Add a new section to README.md immediately after the Installation section (or before the Development section, whichever is more logical for the existing structure).

    Insert this Markdown block:

    ```markdown
    ## Performance acceleration (optional)

    `pip install spike` ships with a NumPy-only backend that passes the full
    bit-exact regression suite. For wall-clock acceleration on GTX NPU compute
    kernels, install the optional `[fast]` extras:

    ```bash
    pip install spike[fast]
    ```

    This installs [numba](https://numba.pydata.org/) (LLVM JIT compiler) and
    enables `@njit(cache=True)` on the 28 stateless GTX kernels (`gemm_core`,
    `vec_core` 7 ops, `act_core` 18 ops). The integration is **transparent** —
    the same Python API works whether numba is installed or not. The first
    invocation pays a one-shot compile cost (~640 ms per kernel; ~17.9 s
    aggregate); subsequent runs hit the disk cache (`__pycache__/*.nbi`).

    Empirical speedups (verified on x86_64 manylinux2014):
    - `gemm_core` 16x16x16 FP16: ~455x (910 µs → 2 µs)
    - Full vendor 84-op `n1s16` regression sweep: >= 5x

    **Bit-exactness is preserved** — `fastmath=False` (numba default) +
    explicit FP32 Python for-loop accumulate + `with numba.objmode(...)`
    escape for 5 transcendental kernels (gelu, tanh_act, sigmoid, softmax,
    esum) match NumPy oracle byte-for-byte (ULP-0 parity). Per-kernel parity
    test: `pytest tests/gtx/test_njit_parity.py -v`.

    **Disable acceleration** without uninstalling:
    ```bash
    # Either uninstall numba, or set the env var (next major version):
    pip uninstall numba
    # The `HAS_NUMBA = False` path takes over automatically.
    ```
    ```

    Insertion point: prefer placement after the existing `## Installation` section. If no `## Installation` section exists, place after the introductory paragraph (before any other major section).
  </action>
  <verify>
    <automated>grep -c "Performance acceleration" README.md &amp;&amp; grep -c "pip install spike\\[fast\\]" README.md &amp;&amp; grep -c "ULP-0 parity" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Performance acceleration" README.md` returns >= 1.
    - `grep -c "pip install spike\\[fast\\]" README.md` returns >= 1.
    - `grep -c "numba" README.md` returns >= 2 (in section body).
    - `grep -c "@njit" README.md` returns >= 1.
    - `grep -c "ULP-0" README.md` returns >= 1.
    - `grep -c "fastmath=False" README.md` returns >= 1.
    - `grep -c "objmode" README.md` returns >= 1.
  </acceptance_criteria>
  <done>README.md "Performance acceleration" section added; explains [fast] extras opt-in, transparent fallback, bit-exact preservation.</done>
</task>

</tasks>

<verification>
**Phase 7 Plan 06 verification commands:**

```bash
# REQUIREMENTS.md sync
grep -q "v1 hard dependency 제외" .planning/REQUIREMENTS.md
grep -c "^- \\[ \\] \\*\\*NJIT-" .planning/REQUIREMENTS.md   # expected 8
grep -q "v1 requirements: 50 total" .planning/REQUIREMENTS.md

# PROJECT.md sync
grep -q "base wheel size" .planning/PROJECT.md

# ROADMAP.md sync
grep -q "NJIT-01" .planning/ROADMAP.md
grep -q "NJIT-08" .planning/ROADMAP.md
grep -c "07-01-PLAN.md" .planning/ROADMAP.md   # expected 1

# README.md sync
grep -q "pip install spike\\[fast\\]" README.md
grep -q "Performance acceleration" README.md

# pyproject.toml CI sync
grep -q "test-extras = \\[\"fast\"\\]" pyproject.toml
python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'fast' in d['tool']['cibuildwheel']['test-extras']"

# Combined doc sync verify (one-liner from CONTEXT acceptance)
grep -q 'optional .spike\\[fast\\] extras' .planning/REQUIREMENTS.md && \
grep -q 'base wheel size' .planning/PROJECT.md && \
grep -q 'NJIT-' .planning/REQUIREMENTS.md && \
grep -q 'pip install spike\\[fast\\]' README.md && \
echo "doc sync OK"
# Expected: prints "doc sync OK"
```
</verification>

<success_criteria>
- [ ] REQUIREMENTS.md Out of Scope numba row reworded; NJIT-01..NJIT-08 section added; Traceability extended; Coverage updated to 50/50
- [ ] PROJECT.md "wheel size" clarified to "base wheel size ≤50MB"
- [ ] ROADMAP.md Phase 7 section filled with goal/requirements/success criteria/plans (no `[To be planned]` placeholder remains)
- [ ] README.md adds "Performance acceleration" section explaining `pip install spike[fast]`
- [ ] pyproject.toml adds `[tool.cibuildwheel] test-extras = ["fast"]`
- [ ] All 5 doc sync verifications pass via the combined grep one-liner
</success_criteria>

<output>
After completion, create `.planning/phases/07-numba/07-06-SUMMARY.md`. requirements-completed: NJIT-08 (doc sync — REQUIREMENTS.md / PROJECT.md / ROADMAP.md / README.md all updated) + NJIT-07 (CI verification half: cibuildwheel test-extras = ["fast"] landed; the extras declaration was completed by Plan 01).

This SUMMARY also serves as the Phase 7 closing summary — at this point all 8 NJIT requirements are landed (assuming Plans 02-05 are green). Final ROADMAP.md status update from "Pending" to "Complete" for NJIT-01..NJIT-08 happens in `/gsd:complete-phase 7` (separate workflow).
</output>
