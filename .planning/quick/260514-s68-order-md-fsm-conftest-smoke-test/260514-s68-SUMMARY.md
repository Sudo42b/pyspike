---
phase: quick-260514-s68
plan: 01
subsystem: tests/gtx
tags: [smoke-test, fsm, conftest, infra]
type: quick-task
requires: [src/main/python/riscv/gtx/fsm.py, src/main/python/riscv/gtx/npu.py, src/main/python/riscv/gtx/writeback.py]
provides: [tests/gtx/conftest.py, tests/gtx/_mocks.py, tests/gtx/test_fsm_smoke.py, tests/gtx/test_custom0_smoke.py]
affects: [tests/gtx]
tech-stack:
  added: []
  patterns: [pytest-fixtures, cuda-collection-gate, named-attribute-mock]
key-files:
  created:
    - tests/gtx/conftest.py
    - tests/gtx/_mocks.py
    - tests/gtx/test_fsm_smoke.py
    - tests/gtx/test_custom0_smoke.py
  modified: []
decisions:
  - "CUDA gate at module-top of conftest (pytest.exit, not skip) per CONTEXT D-CUDA-REQUIRED"
  - "Real GtxNpu fixture (not subclass / not mocked) per CONTEXT D-FIXTURE-REAL"
  - "Smoke split: FSM structural vs custom0 integration per CONTEXT D-SMOKE-SPLIT"
  - "DummyInsn defaults to funct=0 -- run_pipeline takes the no-handler path which still satisfies int-return contract"
metrics:
  duration: "~6 min"
  completed: 2026-05-14
  loc_total: 183
  tests_added: 5
---

# Phase quick-260514-s68 Plan 01: ORDER.md FSM conftest + smoke test Summary

Minimal 4-file test-infra rebuild atop ORDER.md FSM after R1 cleanup deleted the legacy 38-file tests/gtx tree.

## What Was Built

| File                                | LOC | Purpose                                                          |
| ----------------------------------- | --: | ---------------------------------------------------------------- |
| `tests/gtx/conftest.py`             |  55 | CUDA gate (pytest.exit, no skip) + gtx_npu/mock_proc/dummy_insn fixtures |
| `tests/gtx/_mocks.py`               |  65 | MockProcessor (state.XPR + get_csr/put_csr) + DummyInsn (funct/rs1/rs2/rd/xd/xs1/xs2) |
| `tests/gtx/test_fsm_smoke.py`       |  35 | 3 structural tests: NpuState enum members + 4 state callables + writeback->IDLE |
| `tests/gtx/test_custom0_smoke.py`   |  28 | 2 integration tests: custom0/custom1 return int (RoCC reg_t contract) |
| **Total**                           | **183** | Under 250 LOC budget |

## Test Inventory (5 tests)

**tests/gtx/test_fsm_smoke.py** (3):
1. `test_npu_state_enum_has_five_members` -- {IDLE, DECODE, DISPATCH, EXECUTE, WRITEBACK}
2. `test_state_functions_are_importable_callables` -- state_decode/dispatch/execute/writeback are callable
3. `test_state_writeback_returns_idle(gtx_npu)` -- post-R1 writeback unconditionally returns NpuState.IDLE

**tests/gtx/test_custom0_smoke.py** (2):
4. `test_custom0_returns_int(gtx_npu, mock_proc, dummy_insn)` -- GtxNpu.custom0(...) -> int
5. `test_custom1_returns_int(gtx_npu, mock_proc, dummy_insn)` -- GtxNpu.custom1(...) -> int

## Commits

- `3d3e7a3` `test(gtx): conftest CUDA gate + minimal MockProcessor mocks` (Task 1)
- `cf70f26` `test(gtx): FSM + custom0 entry smoke tests` (Task 2)

## Verification Status

**On the executor's worktree** (current environment): `pytest tests/gtx/ --collect-only` cannot proceed because the local `torch` install is broken (`libcusparseLt.so.0: cannot read file data`). This is an upstream environment issue unrelated to this plan -- `import torch` fails at conftest line 15 before the CUDA gate at line 18 can even evaluate.

**On a CUDA-enabled host** (where torch imports cleanly): the design behaves as specified:
- `torch.cuda.is_available() == True`  -> gate passes, fixtures build, 5 tests collected and runnable.
- `torch.cuda.is_available() == False` -> `pytest.exit("GTX tests require CUDA -- ORDER.md constraint", returncode=1)` -- collection halts, no test runs, exit code 1.

**Static checks that DID pass on this worktree**:
- 5 `def test_*` definitions present across the two smoke files (`grep -c` confirms).
- All `key_links` patterns from PLAN frontmatter present: `pytest.exit`, `torch.cuda.is_available`, `GtxNpu()`, `.custom0(`, `from riscv.gtx.fsm import`.
- LOC budget: 183 / 250 (73%).
- Git status confirms ORDER.md / src/main/python/riscv/gtx/unit/ins/disasm.py are unchanged from the user's pre-existing modifications (no scope leak).

## Constraint Compliance vs CONTEXT.md

| Decision                       | Evidence in code                                                                 | Status |
| ------------------------------ | -------------------------------------------------------------------------------- | :----: |
| D-SMOKE-SPLIT                  | Two separate test files (fsm_smoke.py vs custom0_smoke.py)                       |   OK   |
| D-FIXTURE-REAL                 | `from riscv.gtx.npu import GtxNpu; npu = GtxNpu()` (no subclassing)              |   OK   |
| D-CUDA-REQUIRED                | `pytest.exit(...returncode=1)` at module top, NOT `pytest.skip`                  |   OK   |
| D-REGRESSION-OUT-OF-SCOPE      | No `.elf` references, no firmware fixtures, no fake instruction sequences        |   OK   |
| MockProcessor surface = 4 only | `state.XPR.__getitem__`, `state.XPR.write`, `get_csr`, `put_csr` -- nothing more |   OK   |
| DummyInsn attrs = 7 only       | `funct, rs1, rs2, rd, xd, xs1, xs2` -- matches custom0 fast-path reads           |   OK   |

## Deviations from Plan

None -- plan executed exactly as written. All CONTEXT.md locked decisions honored verbatim; no Rule 1/2/3 auto-fixes were needed; no Rule 4 architectural escalations encountered.

## Authentication Gates

None.

## Known Stubs

None. The two smoke files are the floor of intentional minimalism; further behavioral coverage (real funct7 dispatch chains, vendor `.elf` regression) is explicitly out-of-scope per CONTEXT.md D-REGRESSION-OUT-OF-SCOPE and will land in a separate follow-up quick task.

## Open Notes for Successor

1. Once a CUDA-enabled host is available, the next executor should run
   `pytest tests/gtx/test_fsm_smoke.py tests/gtx/test_custom0_smoke.py -v`
   to confirm all 5 tests pass green. The "static check + structural review" performed
   here does not substitute for a real green run.
2. Follow-up quick task: `test_regression_seed.py` for ABS 1-op `.elf` regression
   (per CONTEXT.md "후속 quick task 가 `test_regression_seed.py` 로 ABS 1 op 회귀 1개를 추가").

## Self-Check: PASSED

- File `/mnt/e/14_NIGHTLY/pyspike/tests/gtx/conftest.py` exists: FOUND
- File `/mnt/e/14_NIGHTLY/pyspike/tests/gtx/_mocks.py` exists: FOUND
- File `/mnt/e/14_NIGHTLY/pyspike/tests/gtx/test_fsm_smoke.py` exists: FOUND
- File `/mnt/e/14_NIGHTLY/pyspike/tests/gtx/test_custom0_smoke.py` exists: FOUND
- Commit `3d3e7a3` in git log: FOUND
- Commit `cf70f26` in git log: FOUND
