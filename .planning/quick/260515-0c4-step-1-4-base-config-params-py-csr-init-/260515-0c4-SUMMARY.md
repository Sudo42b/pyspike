---
quick_id: 260515-0c4
date: 2026-05-15
status: complete
test_gate: 26/26 PASS
commits:
  - 1d6cd9a (A — base 재정의 제거)
  - e8a5f25 (B — dma.py GSPR_GTX_OPERAND3 import uncomment)
files_changed:
  - src/main/python/riscv/gtx/config_params.py (-11 lines)
  - src/main/python/riscv/gtx/unit/context/spr_router.py (+5 -2)
  - src/main/python/riscv/gtx/unit/context/dma.py (+3 -3, net 0)
loc_total: "-13 +8 = -5 net"
---

# Quick Task 260515-0c4: Step 1/4 base 재정의 제거 + dma.py NameError fix

## What changed

**A — base 상수 재정의 제거**

`config_params.py:41-51` 의 `GSPR_BASE/END`, `NSPR_BASE/END`, `LSPR_BASE/END`
6 상수 + 5-line docstring header 를 모두 삭제하고, csr/__init__.py:42-47 을
single source of truth 로 굳혔다. 유일한 consumer 인 `spr_router.py:27-28`
의 import 를 두 줄로 분리:

```python
from ...config_params import GTX_NEST_NUM, GTX_SPU_NUM
from ..csr import (GSPR_BASE, GSPR_END, NSPR_BASE, NSPR_END,
                   LSPR_BASE, LSPR_END)
```

**B — dma.py NameError fix**

`dma.py:18-27` import block 에 `GSPR_GTX_OPERAND3` 가 d6f73f9 이후 주석으로
남아 있었으나, `dma.py:70/101/132` 의 firmware_dma load/store/copy 핸들러가
실제로 `npu.gspr.get(GSPR_GTX_OPERAND3, 0)` 로 호출한다. 260514-vjk 가
encoding.py 에 re-export 까지 했지만 dma.py 의 import 주석을 안 풀었다.
주석 한 줄 해제 (`#` 한 글자 제거 + 정렬). 다른 두 주석된 import 그룹
(`LSPR_SPM_ADDRA/R`, `GTX_ISS_F7_DMA_TPOSE/FILL`) 은 Step 2/3 으로 미룸.

## Why

사용자가 명시:
- "재정의한 부분이 있어" — A
- d6f73f9 발생 잠재 NameError 정리 흐름의 첫 surgical 단계 — B

각 commit 독립 atomic; 사용자가 "Step별 별도 quick task (입자적 안전)" 선택.

## Test gate

```bash
uv run pytest tests/gtx/test_fsm_smoke.py tests/gtx/test_custom0_smoke.py \
              tests/gtx/test_csr_registry_chain.py tests/gtx/test_custom_dispatch_chain.py -v
```

**A 적용 후:** 26 passed.
**B 적용 후:** 26 passed.

회귀 없음 — Step 2/3/4 가 건드릴 KeyError 잠재 site 는 본 task 가 안 닿음.

## Out of scope (Step 2/3/4 로 미룸)

- **Step 2 — KeyError fix**: mm.py `CSR_LSPR['LSPR_SPM_ADDR_A']` × 12 callsite
  + act.py `LSPR["LSPM_SPM_ADDRA"]` typo × 다수 + `GSPR["GSPR_GTX_OPCODE"]`
  (등록 안 됨)
- **Step 3 — 패턴 통일**: RegisterView attribute access (`npu.gspr.GSPR_GTX_OPERAND3`)
  로 mm/vec/act 통일 + dma.py / vec.py 의 `LSPR_SPM_ADDRA/R` bare-int 사용 정리
- **Step 4 — silent-clamp → assert**: vec.py:245, dma.py:42

## Open notes for successor (Step 2)

- act.py 에 typo 다수: `LSPM_SPM_ADDRA` (305), `LSPM_SPM_ADDRR` (306),
  `LSPMSPM_ADDRA` (465), `LSPR_SPM_ADDRA/R` (432, 433, 466).
- 실제 csr/lspr.py 등록된 PIPE 이름: `SPM_ADDRA` / `SPM_ADDRB` / `SPM_ADDRC` /
  `SPM_ADDRR` (prefix 없음).
- mm.py 가 사용하는 `LSPR_SPM_ADDR_A/_B/_C/_R` 는 underscore 첨가 변형 — csr
  에 없음. 모두 `SPM_ADDRA/B/C/R` 로 정렬해야.
- `GSPR_GTX_OPCODE` 는 csr/gspr.py 에 등록 안 됨. act.py:598/609/627/638 가
  사용 — KeyError. Step 2 또는 별도 task 에서 register 등록 + Python 매핑
  주소 확정 (vendor 0x004 는 OPERAND4 와 충돌; d6f73f9 이전 Python 매핑은
  OPCODE @ 0x012).
